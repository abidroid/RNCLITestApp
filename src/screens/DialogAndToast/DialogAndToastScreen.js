import { View, Text, TouchableOpacity } from "react-native";
import { Dialog, Toast, ALERT_TYPE } from "react-native-alert-notification";

const DialogAndToastScreen = () => {
    return (
        <View>

        </View>
    );
};

export default DialogAndToastScreen;